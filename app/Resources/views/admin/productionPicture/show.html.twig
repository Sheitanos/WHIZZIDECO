{% extends 'base.html.twig' %}

{% block body %}
    <h1>Productionpicture</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productionPicture.id }}</td>
            </tr>
            <tr>
                <th>Imagename</th>
                <td>{{ productionPicture.imageName }}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{% if productionPicture.updatedAt %}{{ productionPicture.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('productionPicture_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('productionPicture_edit', { 'id': productionPicture.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
