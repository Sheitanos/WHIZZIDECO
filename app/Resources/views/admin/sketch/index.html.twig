{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <section class="container-fluid">
        <h1 class="text-center title-realisation">Mes croquis page d'accueil</h1>
        <div class="row sketchs-content">
            {% for sketch in sketches %}
                <div class="col-xs-6 col-md-4 productionPicture">
                    <a href="{{ path('sketch_edit', { 'id': sketch.id }) }}" class="thumbnail">
                        <img src="{{ asset( 'uploads/sketch/' ~ sketch.imageName) }}" alt="image">
                    </a>
                    <div class="col-md-offset-4 col-md-5">
                        {{ form_start(delete_form[sketch.id]) }}
                        <input type="submit" class="btn btn-danger"  value="Supprimer"/>
                        {{ form_end(delete_form[sketch.id]) }}
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}
